import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    type: 'dark',
    background: {
      default: '#191A1C',
      paper: '#2f3237',
    },
    text: {
      primary: '#ffffff',
      secondary: '#00000',
    },
  },
});
