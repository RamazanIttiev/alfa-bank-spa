import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    main: '#FFF59D',
    type: 'dark',
    background: {
      default: '#191A1C',
      paper: '#2f3237',
    },
    text: {
      primary: '#ffffff',
      secondary: '#00000',
    },
  },
});
